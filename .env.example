# Production (Vercel) — bắt buộc cho lưu file bền vững giữa các request
# Tạo Blob store: Vercel Dashboard → Storage → Blob → Connect
# BLOB_STORE_ID_READ_WRITE_TOKEN=

# Dev local không cần token — dùng thư mục .cache/
